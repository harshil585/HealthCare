spring.application.name=healthcare-system
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/healthcare_system}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:202351064}
spring.datasource.driver-class-name=${DB_DRIVER:com.mysql.cj.jdbc.Driver}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=${DB_DIALECT:org.hibernate.dialect.MySQLDialect}

# Spring Mail Settings (Gmail SMTP Configuration)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:kerharshil0502@gmail.com}
spring.mail.password=${MAIL_PASSWORD:oqwb galn qbaf bgia}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Razorpay Integration Config Toggles
razorpay.mode=${RAZORPAY_MODE:simulated}
razorpay.key.id=${RAZORPAY_KEY_ID:mock_key}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:mock_secret}

# Twilio SMS Integration Config Toggles
twilio.mode=${TWILIO_MODE:real}
twilio.account.sid=${TWILIO_ACCOUNT_SID:AC713588ecc925b7eae65f5a3652f73b73}
twilio.auth.token=${TWILIO_AUTH_TOKEN:7044633204add8ae846b470a3553e5d9}
twilio.phone.number=${TWILIO_PHONE_NUMBER:+918799409795}

# Google OAuth 2.0 Configuration
google.client.id=${GOOGLE_CLIENT_ID:964766246391-0fqufcfck945e286k5ojpkfclq8jfa9m.apps.googleusercontent.com}

# Notification Logging
logging.level.com.healthcare.healthcare_system.service.NotificationService=DEBUG
logging.level.com.healthcare.healthcare_system.service.AuthService=DEBUG
